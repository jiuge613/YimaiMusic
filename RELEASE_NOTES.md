# Yimai v0.1.9

精美的高性能桌面音乐播放器（Rust + Tauri 2 + React）。

## 本次更新

- **音源脚本校验放宽**：导入本地音源脚本时不再要求引用 `globalThis.lx`、也不再强制 `musicUrl` 动作名。只要脚本具备取链方法、音源接口路径、平台声明或接口地址常量之一，并能解析出取链接口地址，即可成功导入；普通音源脚本（如 source.js）不再被误拦。
- **保留必要的错误提示**：网页 / JSON / XML 等非脚本文档、结构不像音源脚本、解析不出取链接口地址三类情况仍给出明确的中文报错。
- **LX 脚本行为不变**：原有 LX 音源脚本（引用 `globalThis.lx`）的解析结果与改动前完全一致，平台与音质声明照常识别。
- **修复排行榜播放失败**：修复点击搜索结果播放时提示 `missing required key req` 的问题；未指定平台且未接入音源时自动回退酷狗匿名取链。

## 功能概览

- 本地曲库管理与扫描
- 排行榜搜索与在线播放
- 音源管理（本地脚本 / 网络接口音源导入、启停与删除）
- 网易云 / QQ 音乐在线曲库
- QRC 逐字歌词与桌面歌词窗口
- 10 段均衡器（biquad DSP）
- 主题与皮肤切换、系统托盘与 SMTC 媒体控制

## 安装说明

1. 下载下方 `Yimai_*_x64-setup.exe`。
2. 双击运行，按向导完成安装（默认安装到当前用户目录，可切换为所有用户）。
3. 启动后可在「设置 → 检查更新」中验证自动更新链路。

## 说明

- 仅提供 Windows x64 安装包。
- 排行榜结果来自你自行接入的音源或内置平台，请仅播放你有权使用的音乐。
- 本项目基于 LingyunStudio/RustMusic（Apache-2.0）二次开发，版权声明保留在原 LICENSE 中。
