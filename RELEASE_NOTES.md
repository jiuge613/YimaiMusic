# Yimai v0.1.10

精美的高性能桌面音乐播放器（Rust + Tauri 2 + React）。

## 本次更新

- **修复混淆 / 服务端下发音源脚本导入误判**：洛雪 / 落雪系「服务端下发」脚本（如 `lx-music-source-v6+`）主体经过 JS Packer 混淆，静态文本里看不到 `musicUrl` / `url.php` / `wy:` 等特征，也不引用 `globalThis.lx`，此前会被误报「文件结构不像音源脚本」。现在通过 `SERVER_SCRIPT_CONFIG` + `apiUrl` 下发配置签名识别这类脚本，并从容器的 `apiUrl` 抽出取链接口基址，导入即可成功。
- **导入校验兼容性增强**：`apiUrl` 提取兼容 JSON 转义斜杠（`https:\/\/host`）与 JS 赋值写法；未带该签名的随机文件仍会被正确拒绝。
- **LX 导入音源支持歌词**：新增 `lx_lyric` 取词命令（请求 `{base}/lyric.php`），播放 LX 音源曲目时后端携带音源身份（音源 id / 平台 / 歌曲 id），前端自动回查并展示歌词（含逐字高亮与桌面歌词）。
- **修复酷狗在线曲库无歌词**：`loadLyricsByKey` 此前未把 `kug-` 键路由到 `kugou_lyric`，导致酷狗曲目也一直显示「暂无歌词」，现已接通。

## 功能概览

- 本地曲库管理与扫描
- 排行榜搜索与在线播放
- 音源管理（本地脚本 / 网络接口音源导入、启停与删除）
- 网易云 / QQ 音乐在线曲库
- QRC 逐字歌词与桌面歌词窗口
- 10 段均衡器（biquad DSP）
- 主题与皮肤切换、系统托盘与 SMTC 媒体控制

## 安装说明

1. 下载下方 `Yimai_*_x64-setup.exe`。
2. 双击运行，按向导完成安装（默认安装到当前用户目录，可切换为所有用户）。
3. 启动后可在「设置 → 检查更新」中验证自动更新链路。

## 说明

- 仅提供 Windows x64 安装包。
- 排行榜结果来自你自行接入的音源或内置平台，请仅播放你有权使用的音乐。
- 本项目基于 LingyunStudio/RustMusic（Apache-2.0）二次开发，版权声明保留在原 LICENSE 中。
