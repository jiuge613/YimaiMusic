# Yimai v0.1.13

精美的高性能桌面音乐播放器（Rust + Tauri 2 + React）。

## 本次更新

- **歌词备用源兜底**：新增 `backup_lyric` 命令。当本地 `.lrc` / 内嵌标签 / 平台接口都拿不到歌词时，按「歌名 + 歌手」到网易云检索并评分匹配，取最高分结果拉取歌词（优先 yrc 逐字，退回 LRC）。前端 `loadLyricsByKey` 在主源零行时自动走兜底，彻底解决「无歌词」。
- **播放流畅度优化**：解码路径的 `BufReader` 从默认 8KB 提升到 1MiB，大幅降低大文件（FLAC）解码线程的 syscall 频率（约 128×），修复全音源播放偶发卡顿。
- **本地音乐「移出资料库」**：资料库 / 我喜欢 / 最近播放中的本地曲目支持「移出资料库」（悬停垃圾桶按钮或右键菜单），仅移除记录、不删磁盘文件；再次扫描命中同路径时自动恢复。
- **排行榜「喜欢」按钮**：排行榜搜索结果每首歌新增「喜欢」按钮，一键收藏进导航栏「我喜欢」模块（`kind="lx"`，rid 形如 `音源id:平台:歌id`，取链上下文存入 mediaMid）。我喜欢页 / 播放列表的 `lx` 条目可正常二次播放。
- **更新下载集成 GitHub 镜像**：更新包下载在原 GitHub Release 直连失败时，自动轮询 19 个国内镜像代理（ghproxy / gitclone 等），缓解国内网络下载频繁失败。

## 功能概览

- 本地曲库管理与扫描、移出资料库（软删除记录）
- 排行榜搜索与在线播放、一键「喜欢」入库
- 音源管理（本地脚本 / 网络接口音源导入、启停与删除）
- 网易云 / QQ 音乐 / 酷狗在线曲库
- QRC 逐字歌词、备用源兜底与桌面歌词窗口
- 10 段均衡器（biquad DSP）
- 大缓冲解码，播放更顺滑
- GitHub 镜像加速的自动更新
- 主题与皮肤切换、系统托盘与 SMTC 媒体控制

## 安装说明

1. 下载下方 `Yimai_*_x64-setup.exe`。
2. 双击运行，按向导完成安装（默认安装到当前用户目录，可切换为所有用户）。
3. 启动后可在「设置 → 检查更新」中验证自动更新链路。

## 说明

- 仅提供 Windows x64 安装包。
- 排行榜结果来自你自行接入的音源或内置平台，请仅播放你有权使用的音乐。
- 本项目基于 LingyunStudio/RustMusic（Apache-2.0）二次开发，版权声明保留在原 LICENSE 中。
